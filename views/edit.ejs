<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Patient - MediCloud</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5" style="max-width: 600px;">
        <div class="card shadow border-0">
            <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Update Record</h5>
                <small class="text-white-50">ID: <%= patient._id.toString().slice(-6) %></small>
            </div>
            <div class="card-body p-4">
                <form action="/edit/<%= patient._id %>" method="POST">
                    
                    <h6 class="text-primary border-bottom pb-2 mb-3">Patient Info</h6>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">FULL NAME</label>
                        <input type="text" class="form-control" name="fullName" value="<%= patient.fullName %>" required>
                    </div>
                    <div class="row mb-3">
                        <div class="col-6">
                            <label class="form-label small fw-bold">AGE</label>
                            <input type="number" class="form-control" name="age" value="<%= patient.age %>" required>
                        </div>
                        <div class="col-6">
                            <label class="form-label small fw-bold">ROOM</label>
                            <input type="text" class="form-control" name="roomNumber" value="<%= patient.roomNumber %>">
                        </div>
                    </div>

                    <!-- NEW: Vitals Section -->
                    <h6 class="text-danger border-bottom pb-2 mb-3 mt-4">Current Vitals</h6>
                    <div class="row mb-3">
                        <div class="col-4">
                            <label class="form-label small fw-bold">HEART RATE</label>
                            <input type="number" class="form-control" name="vitals[heartRate]" value="<%= patient.vitals ? patient.vitals.heartRate : 0 %>">
                        </div>
                        <div class="col-4">
                            <label class="form-label small fw-bold">BP</label>
                            <input type="text" class="form-control" name="vitals[bloodPressure]" value="<%= patient.vitals ? patient.vitals.bloodPressure : 'N/A' %>">
                        </div>
                        <div class="col-4">
                            <label class="form-label small fw-bold">TEMP (°C)</label>
                            <input type="number" step="0.1" class="form-control" name="vitals[temperature]" value="<%= patient.vitals ? patient.vitals.temperature : 0 %>">
                        </div>
                    </div>

                    <h6 class="text-primary border-bottom pb-2 mb-3 mt-4">Clinical Details</h6>
                    
                    <div class="mb-3">
                        <label class="form-label small fw-bold">DEPARTMENT</label>
                        <select class="form-select" name="department">
                            <% const depts = ['Emergency', 'ICU', 'Cardiology', 'Pediatrics', 'Neurology', 'General Ward']; %>
                            <% depts.forEach(d => { %>
                                <option value="<%= d %>" <%= patient.department === d ? 'selected' : '' %>><%= d %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-bold">CONDITION</label>
                        <input type="text" class="form-control" name="condition" value="<%= patient.condition %>" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label small fw-bold">STATUS</label>
                        <select class="form-select" name="status">
                            <option value="Admitted" <%= patient.status === 'Admitted' ? 'selected' : '' %>>Admitted</option>
                            <option value="Critical" <%= patient.status === 'Critical' ? 'selected' : '' %>>Critical</option>
                            <option value="Recovery" <%= patient.status === 'Recovery' ? 'selected' : '' %>>Recovery</option>
                            <option value="Outpatient" <%= patient.status === 'Outpatient' ? 'selected' : '' %>>Outpatient</option>
                            <option value="Discharged" <%= patient.status === 'Discharged' ? 'selected' : '' %>>Discharged</option>
                        </select>
                    </div>

                    <div class="mb-4">
                        <label class="form-label small fw-bold">NOTES</label>
                        <textarea class="form-control" name="notes" rows="3"><%= patient.notes %></textarea>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/" class="btn btn-light border px-4">Cancel</a>
                        <button type="submit" class="btn btn-primary px-4">Save Updates</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>